#include 'main.h'

/**
 *main - Entry point
 *
 * Description: checks if a character is lowercase
 *
 * Return: 1 if letter is lowercase, 0 if not
 */
int main(void)
{
if (c >= 'a' && c <= 'z')
return (1);
else
	return (0);
}
