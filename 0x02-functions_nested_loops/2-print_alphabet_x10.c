#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'prints 10 times the alphabet'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ten;
	char c;

	for (ten = 0; ten < = 9; ten++)
{
	for (c = 'a'; c < = 'z'; c++)
	_putchar(c);
	_putchar('\n');
}
return (0);
}
