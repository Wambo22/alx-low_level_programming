#include 'main.h'
/**
 * main - Entry point
 *
 * Describe: checks for alphabetic characters
 *
 * Return  Always 0 (Success)
 */
void checks alphabets(void)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'z'))
return (1);
else
return (0);
}
